<link rel="shortcut icon" href="{{ preloaded.config.virtualDirectory }}/images/favicon.ico?{{ package.version }}" />
{{#if environments.production}}
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/vendor.min.css?{{ package.version }}" />
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/atigeo-base.min.css?{{ package.version }}" />
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/beanstack.min.css?{{ package.version }}" />
{{else}}
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/vendor.css?{{ package.version }}" />
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/atigeo-base.min.css?{{ package.version }}" />
    <link rel="stylesheet" href="{{ preloaded.config.virtualDirectory }}/dist/css/beanstack.css?{{ package.version }}" />
{{/if}}